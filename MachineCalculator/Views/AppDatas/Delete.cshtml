@model MachineCalculator.Models.AppData

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>AppData</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(modelItem => Model.name)
        </dt>
        <dd>
            @Html.DisplayFor(modelItem => Model.name)
        </dd>
        <dt>
            @Html.DisplayNameFor(modelItem => Model.instances)
        </dt>
        <dd>
            @Html.DisplayFor(modelItem => Model.instances)
        </dd>
        <dt>
            @Html.DisplayNameFor(modelItem => Model.flag)
        </dt>
        <dd>
            @Html.DisplayFor(modelItem => Model.flag)
        </dd>
        @foreach (var subitem in Model.resourses)
        {
            <dt>
                @Html.DisplayFor(modelItem => subitem.name)
            </dt>
            <dd>
                @Html.DisplayFor(subModelItem => subitem.load)
            </dd>
        }
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
